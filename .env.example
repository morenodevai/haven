# Haven Server Configuration
# Copy to .env and fill in values

# JWT secret for auth tokens — CHANGE THIS in production
HAVEN_JWT_SECRET=change-me-to-a-random-string

# Database file path
HAVEN_DB_PATH=haven.db

# Server bind address
HAVEN_HOST=0.0.0.0
HAVEN_PORT=3000
