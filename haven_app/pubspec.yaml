name: haven_app
description: "Haven 3.0 — Encrypted communication, reimagined."
publish_to: 'none'
version: 3.1.0+5

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # HTTP client
  dio: ^5.8.0

  # WebSocket
  web_socket_channel: ^3.0.2

  # Secure storage (OS keychain)
  flutter_secure_storage: ^9.2.4

  # AES-256-GCM encryption
  pointycastle: ^3.9.1

  # File picker
  file_picker: ^8.1.7

  # Platform directories
  path_provider: ^2.1.5

  # Navigation
  go_router: ^14.8.1

  # JSON serialization
  json_annotation: ^4.9.0

  # UUID generation
  uuid: ^4.5.1

  # Collection utilities
  collection: ^1.19.1

  # Intl for date formatting
  intl: ^0.20.2

  # URL launcher (for links in chat)
  url_launcher: ^6.3.1

  # FFI for native file client
  ffi: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  json_serializable: ^6.9.4

flutter:
  uses-material-design: true
